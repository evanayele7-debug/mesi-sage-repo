import { Text, View } from "react-native";

export default function DetalisScreens () {
    return ( <View >
        <Text  style={{backgroundColor:'red'}} > Detalis page </Text>
    </View>

    );
}